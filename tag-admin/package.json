{
  "name": "tag-admin",
  "private": true,
  "license": "BSD",
  "scripts": {
    "test": "jest",
    "build": "rollup -c",
    "build:prod": "rollup -c --environment BUILD:production",
    "watch": "rollup -c --watch --no-watch.clearScreen"
  },
  "dependencies": {
    "react": "^16.14.0",
    "react-dom": "^16.14.0",
    "react-table": "^6.11.5"
  }
}
