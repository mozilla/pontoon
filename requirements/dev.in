# This requirements file only contains direct dependencies
# that are used in development environments only.
#
# When making changes to any requirements/*.in file,
# recompile the requirements with:
# - `make requirements` if using the development Docker environment
# - `./docker/compile_requirements.sh` if using `uv` directly
#
# The dependencies are sorted by category, then alphabetical order.
# Dependencies that do not come from pypi (eg. eggs from github) are listed at the end of the list.
# -------------------------------------------------------------------------------------------------
-c prod.txt

# Runtime dependencies
-r base.in
# Should match the psycopg2 version of requirements/prod.in.
# Pre-compiled binary version for easier dev env setup.
psycopg2-binary==2.9.12

# Development, testing, and linting dependencies
coverage==7.6.1
django-debug-toolbar==6.3.0
django-extensions==4.1
django-stubs[compatible-mypy]==6.0.5
factory-boy==3.3.1
mypy==2.1.0
pytest-cov==5.0.0
pytest-django==4.9.0
pytest-xdist==3.8.0
pytest==9.0.3
requests-mock==1.12.1
ruff==0.16.1
